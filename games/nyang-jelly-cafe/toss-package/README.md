# nyang-jelly-cafe Toss Package

## commands
- `npm run build:web`
- `npm run build`

## note
- 사운드는 `games/nyang-jelly-cafe/assets/audio/` 또는 `static/sounds/`에서 자동 탐색합니다.
- 파일이 없으면 게임은 fallback tone으로 동작합니다.
