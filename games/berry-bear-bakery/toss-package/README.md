# berry-bear-bakery Toss Package

## commands
- `npm run build:web`
- `npm run build`

## note
- 사운드는 `games/berry-bear-bakery/assets/audio/`에서 자동 탐색합니다.
- 파일이 없으면 게임은 fallback tone으로 동작합니다.
- `toss-bridge.js` 는 `toss-bridge-source.js` 를 `esbuild` 로 번들해 생성합니다.
