# otter-icecream-pop Toss Package

## commands
- `npm run build:web`
- `npm run build`

## note
- 오디오는 선택 사항입니다.
- `games/otter-icecream-pop/assets/audio/` 또는 `static/sounds/`에서 파일을 찾고, 없으면 fallback tone으로 동작합니다.
- `toss-bridge.js`는 `toss-bridge-source.js`를 esbuild로 번들해 생성합니다.
