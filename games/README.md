# Games

Actual project folders live here.

- Open and edit `games/<project>/` directly.
- `games/assets` is a compatibility link for old `../assets` references.
- `games/index.html` points back to the main landing page.
- Root-level same-name folders are compatibility symlinks for legacy URLs.
