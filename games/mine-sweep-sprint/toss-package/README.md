# Mine Sweep Sprint Toss Package

`.ait` 번들 생성을 위한 토스 패키지 폴더입니다.

## 명령어

- `npm run build:web` : 토스 배포용 웹 산출물(`dist/`) 생성
- `npm run dev` : `dist/` 생성 후 로컬 미리보기 서버 실행
- `npm run build` : `.ait` 번들 생성 (`ait build`)
