# Cassette A/B World Toss Package

- 게임 소스: `/games/cassette-ab-world`
- 이 폴더는 앱인토스 업로드용 `.ait` 번들 생성을 위한 패키지입니다.
- `scripts/build-web.mjs`가 `toss-bridge-source.js`를 번들해 `toss-bridge.js`를 갱신합니다.

## Commands

```bash
npm run build:web
npm run dev
npm run build
```
